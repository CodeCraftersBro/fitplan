import 'package:fitplan/features/home/home.dart';
import 'package:fitplan/features/settings/settings.dart';


final routes = {
  "/": (context) => const MainScreen(),
  "/settings": (context) => const SettingsScreen()
};